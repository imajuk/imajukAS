package com.imajuk.ui.window {    import com.imajuk.constructions.DocumentClass;    import com.imajuk.logs.Logger;    import flash.display.StageQuality;    import org.libspark.thread.EnterFrameThreadExecutor;    import org.libspark.thread.Thread;    /**     * @author shinyamaharu     */    public class WindowSampleMain extends DocumentClass     {        public function WindowSampleMain()        {            super(StageQuality.HIGH);                        Thread.initialize(new EnterFrameThreadExecutor());        	Logger.filter(Logger.DEBUG, Logger.RELEASE);        	        	Logger.release("WindowSampleMain start");        }        override protected function start() : void        {        	            new WindowSampleMainThread().start();        };    }}