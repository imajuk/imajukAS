package com.imajuk.ui.buttons{    import com.imajuk.constructions.DocumentClass;    import com.imajuk.logs.Logger;    import org.libspark.thread.EnterFrameThreadExecutor;    import org.libspark.thread.Thread;    import flash.display.StageQuality;    /**     * @author shinyamaharu     */    public class ToggleButtonSample extends DocumentClass    {        public function ToggleButtonSample()        {            super(StageQuality.HIGH);            Thread.initialize(new EnterFrameThreadExecutor());        }        override protected function start() : void        {            Logger.release("ToggleButtonSample");            new ToggleButtonSampleThread(this).start();        };    }}