package com.imajuk.ui.combobox {    import com.imajuk.constructions.DocumentClass;    import com.imajuk.logs.Logger;    import flash.display.StageQuality;    import org.libspark.thread.EnterFrameThreadExecutor;    import org.libspark.thread.Thread;    /**     * @author shinyamaharu     */    public class ComboboxSampleMain extends DocumentClass     {        public function ComboboxSampleMain()        {            super(StageQuality.HIGH);                        Thread.initialize(new EnterFrameThreadExecutor());        }        override protected function start() : void        {            Logger.filter(Logger.WARNING, Logger.INFO, Logger.RELEASE, Logger.DEBUG);            //            Logger.filter(Logger.WARNING, Logger.INFO);            Logger.release("ComboboxSampleMain start");                        new ComboboxSampleMainThread().start();        };    }}